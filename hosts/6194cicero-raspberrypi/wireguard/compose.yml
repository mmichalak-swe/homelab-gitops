networks:
  caddy-net:
    name: caddy-net
    external: true
  wg-ls-net:
    name: wg_net
    driver: bridge
    ipam:
      config:
        - subnet: 172.18.10.0/24
          ip_range: 172.18.10.0/24
          gateway: 172.18.10.1

services:
  wireguard-ls:
    cap_add:
      - CHOWN
      - DAC_OVERRIDE
      - FOWNER
      - NET_ADMIN
    cap_drop:
      - ALL
    container_name: wireguard-ls
    deploy:
      resources:
        limits:
          memory: 256M
    dns: 172.18.0.2
    environment:
      - ALLOWEDIPS=${ALLOWEDIPS}
      - INTERNAL_SUBNET=${INTERNAL_SUBNET}
      - LOG_CONFS=${LOG_CONFS}
      - PEERS=${PEERS}
      - PEERDNS=${PEERDNS}
      - PERSISTENTKEEPALIVE_PEERS=${PERSISTENTKEEPALIVE_PEERS}
      - SERVERPORT=${SERVERPORT}
      - SERVERURL=${SERVERURL}
      - TZ=${TZ}
    healthcheck:
      test: ["CMD-SHELL", "ip link show wg0 &> /dev/null || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    hostname: wireguard-ls
    image: linuxserver/wireguard:1.0.20250521@sha256:33d861fd6ebcdc0f8d946ace22fa1efc2e3fc81951028bca2dea65f40fe05f93
    networks:
      wg-ls-net:
        ipv4_address: 172.18.10.2
        gw_priority: 1
      caddy-net:
        ipv4_address: 172.18.0.6
        gw_priority: 0
    ports:
      - ${SERVERPORT}:51820/udp
    read_only: true
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    sysctls:
      - net.ipv4.conf.all.src_valid_mark=1
    tmpfs:
      - /run:exec
      - /tmp
    volumes:
      - config:/config

volumes:
  config:
